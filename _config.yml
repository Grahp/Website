plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-relative-links
  - jekyll-optional-front-matter
  - jekyll-wikirefs
  - jekyll-redirect-from
  - jekyll-minifier

include:
  - pages
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default

incremental: false
jekyll-minifier:
  remove_intertag_spaces: true
